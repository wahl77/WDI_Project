$('.item_picture_list').append('<%= j render partial:"items" %>')
                                                         
<% if @locations.next_page %>                             
  $('.pagination').remove();
  $('.span12').last().append('<%= j will_paginate @locations %>');
  $('.spinner').remove();
<% else %>                                               
  $('.pagination').remove();
<% end %>

$(window).scroll();
