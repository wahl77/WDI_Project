$('.item_picture_list').append('<%= j render partial:"items" %>')
                                                         
<% if @all.next_page %>                             
  $('.pagination').remove();
  $('.span12').last().append('<%= j will_paginate @all %>');
  $('.spinner').remove();
<% else %>                                               
  $('.pagination').remove();
<% end %>

