%ul#comments
  - comments.each do |comment|
    - if can? :read, comment and comment.id 
      %li.comment
        /= image_tag comment.sender.image.url if comment.sender.image
        %p= comment.content
        .author
          = comment.sender.email if comment.sender
          %br
          = link_to "Delete Comment", comment_path(comment), method: :delete if can? :destroy, comment


:sass

  ul#comments
    padding: 0
    margin: 0
    li.comment
      list-style-type: none
      width: 100%
      border-bottom: 1px solid #dddddd
      background-color: white
      p
        padding: 10px
      .author
        text-align: right
      &:nth-child(2n)
        background-color: #f9f9f9
      img 
        height: 100px
        width: 100px
      
